[project]
name = "fpm_demo_base"
authors = [{ name = "FPM offline test", email = "dev@example.com" }]
description = "FPM Demo Base (fpm offline integration fixture)"
requires-python = ">=3.10"
readme = "README.md"
dynamic = ["version"]
dependencies = [
	"tabulate==0.9.0",
	"msgpack>=1.0",
]

[build-system]
requires = ["flit_core >=3.4,<4"]
build-backend = "flit_core.buildapi"
