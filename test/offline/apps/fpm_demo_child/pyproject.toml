[project]
name = "fpm_demo_child"
authors = [{ name = "FPM offline test", email = "dev@example.com" }]
description = "FPM Demo Child (fpm offline integration fixture)"
requires-python = ">=3.10"
readme = "README.md"
dynamic = ["version"]
dependencies = [
]

[build-system]
requires = ["flit_core >=3.4,<4"]
build-backend = "flit_core.buildapi"
