# FPM Repository Configuration

# Port to expose the repository on the host
FPM_REPO_PORT=8080

# Optional: Add additional environment variables as needed
# NGINX_WORKER_PROCESSES=auto
# NGINX_WORKER_CONNECTIONS=1024
